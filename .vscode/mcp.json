{
    "servers": {
        "trivy-mcp": {
            "type": "stdio",
            "command": "trivy",
            "args": [
                "mcp"
            ],
            "cwd": "${workspaceFolder}"
        }
    }
}